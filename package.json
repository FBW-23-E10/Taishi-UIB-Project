{
  "name": "taishi-uib-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run compile-scss & live-server",
    "compile-scss": "sass --watch src/styles/main.scss src/styles/style.css",
    "build": "rm -rf build && parcel build src/index.html --dist-dir build --public-url ./",
    "publish": "gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.70.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.2"
  }
}
